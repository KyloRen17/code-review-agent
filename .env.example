# 复制为 .env 并填入真实值。.env 已被 .gitignore 忽略，严禁提交真实凭证。
# CLI 启动时自动加载当前目录的 .env（KEY=VALUE，支持 export 前缀与成对引号）；
# 已设置的真实环境变量优先、不会被覆盖；值只进入进程环境，绝不写日志/报告/DB。

# 远程 LLM（Phase 3 接入真实模型时使用）
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Git 平台令牌（Phase 2 读取 / Phase 10 发布时使用）
GITHUB_TOKEN=
GITLAB_TOKEN=
